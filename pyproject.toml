[project]
name = "deep-learning-3e"
version = "0.0.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "keras>=3.13",
  "keras-hub",
  "jupyter",
  "ipykernel",
  "numpy",
  "pandas",
  "matplotlib",
  "seaborn",
  "scikit-learn",
]

[tool.uv]
# We'll keep envs out of the repo; kernels will point to each env anyway.
# (No extra config needed here yet.)
